name: Prepare Render Static Site

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "mdcat_app/**"
      - ".github/workflows/prepare-render-site.yml"

permissions:
  contents: write

concurrency:
  group: render-static-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Check API URL
        env:
          API_BASE_URL: ${{ vars.API_BASE_URL }}
        run: test -n "$API_BASE_URL"
      - name: Build Flutter web app for Render
        working-directory: mdcat_app
        env:
          API_BASE_URL: ${{ vars.API_BASE_URL }}
        run: |
          flutter pub get
          flutter build web --release \
            --base-href "/" \
            --dart-define=API_BASE_URL="$API_BASE_URL"
      - name: Publish compiled site to render-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: render-pages
          publish_dir: ./mdcat_app/build/web
          force_orphan: true
